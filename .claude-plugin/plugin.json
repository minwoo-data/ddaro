{
  "name": "ddaro",
  "version": "0.4.0",
  "description": "Worktree-based parallel workflow for Claude Code sessions with dev-cycle orchestration (new in 0.4.0). 6-tier classification (main / owned / adopted / protected / unmanaged / external), /ddaro:adopt, cwd-safe destructive commands (clear/abandon from main only), main_protection hooks, branch_naming + cross_worktree_check + branch_worktree_match guards. /ddaro:commit gains opt-in --verify flag (verify.json + CLAUDE.md fallback discovery). /ddaro:merge drives the PR path through CI: state machine, polling, hard-capped fix loop (default 3, configurable), CI_STUCK detection, idempotency guard, confirm gate, sync-main content-diff preview, --force-with-lease invariant. New opt-in evidence_check hook (off/warn/strict). Korean triggers: /ddaro, 따로, 병렬로. English: /ddaro, parallel, isolated.",
  "author": {
    "name": "haroom",
    "url": "https://github.com/minwoo-data"
  },
  "homepage": "https://github.com/minwoo-data/ddaro",
  "repository": "https://github.com/minwoo-data/ddaro",
  "license": "MIT",
  "keywords": [
    "claude-code",
    "worktree",
    "parallel",
    "workflow",
    "git",
    "branch",
    "isolation",
    "crash-recovery",
    "adopt",
    "main-protection",
    "ddaro"
  ],
  "commands": "./commands/",
  "skills": "./skills/"
}
